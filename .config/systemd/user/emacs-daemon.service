[Unit]
Description=Emacs daemon

[Service]
Type=forking
ExecStart=/usr/bin/emacs --daemon=systemd
ExecStop=/usr/bin/emacsclient -s systemd --eval '(kill-emacs)'
#Environment=SSH_AUTH_SOCK=%t/keyring/ssh
Restart=always

[Install]
WantedBy=default.target
